spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/geraldobl58/config-data
          username: ${GITHUB_USERNAME}
          password: ${GITHUB_TOKEN}
          clone-on-start: true
          default-label: main
server:
  port: 8888